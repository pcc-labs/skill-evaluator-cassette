# Default variables for the hurl suite, direct against the cassette.
# To run through tapes core instead:
#   root_url stays cassette-only (core probes /ping itself, it never proxies it)
#   cassette_url=http://127.0.0.1:8081/v1/cassettes/skills-evaluator

root_url=http://127.0.0.1:9978
cassette_url=http://127.0.0.1:9978/
